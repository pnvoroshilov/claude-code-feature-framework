# 🤖 AUTO MODE - ПОЛНОСТЬЮ АВТОМАТИЧЕСКИЙ WORKFLOW

**⚠️ ЭТОТ ФАЙЛ - ГЛАВНАЯ ИНСТРУКЦИЯ ДЛЯ AUTO MODE (`manual_mode = false`)**

## 🔴 КРИТИЧЕСКОЕ ПРАВИЛО AUTO MODE

**В AUTO режиме Claude Code должен ДЕЙСТВОВАТЬ НЕМЕДЛЕННО и АВТОМАТИЧЕСКИ.**

- ❌ **НЕ ЖДАТЬ** команд пользователя
- ❌ **НЕ СПРАШИВАТЬ** разрешения на переход между статусами
- ❌ **НЕ ОСТАНАВЛИВАТЬСЯ** после каждого этапа
- ✅ **ВЫПОЛНЯТЬ** весь workflow автоматически от начала до конца
- ✅ **ПЕРЕХОДИТЬ** к следующему статусу сразу после завершения текущего
- ✅ **ЗАПУСКАТЬ** команды автоматически

---

## 🚀 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ ПРИ СТАРТЕ СЕССИИ

```
КОГДА СЕССИЯ НАЧИНАЕТСЯ:
1. СРАЗУ вызвать: mcp__claudetask__get_project_settings
2. ПРОВЕРИТЬ: manual_mode = false?
3. ЕСЛИ manual_mode = false:
   → СРАЗУ вызвать: mcp__claudetask__get_task_queue
   → НАЧАТЬ автоматическую обработку задач
4. НЕ ЖДАТЬ команды пользователя!
```

---

## 📋 ПОЛНЫЙ АВТОМАТИЧЕСКИЙ ЦИКЛ

### Этап 1: BACKLOG → ANALYSIS

**Триггер**: Задача в статусе "Backlog"

**Действия (выполнять НЕМЕДЛЕННО):**
```
1. mcp__claudetask__update_status(task_id, "Analysis")
2. mcp__claudetask__create_worktree(task_id)  # если worktree_enabled
3. Делегировать analysis агентам:
   - business-analyst → requirements.md
   - systems-analyst → architecture.md
4. СРАЗУ после завершения анализа → перейти к Этапу 2
```

### Этап 2: ANALYSIS → IN PROGRESS

**Триггер**: Analysis агенты завершили работу

**Действия (выполнять НЕМЕДЛЕННО):**
```
1. mcp__claudetask__append_stage_result(task_id, "Analysis", "Analysis complete")
2. mcp__claudetask__update_status(task_id, "In Progress")
3. SlashCommand("/start-develop")  # ← ОБЯЗАТЕЛЬНО ВЫПОЛНИТЬ!
4. СРАЗУ после завершения разработки → перейти к Этапу 3
```

### Этап 3: IN PROGRESS → TESTING

**Триггер**: Разработка завершена (коммиты в worktree, агент сообщил о завершении)

**Действия (выполнять НЕМЕДЛЕННО):**
```
1. mcp__claudetask__append_stage_result(task_id, "In Progress", "Implementation complete")
2. mcp__claudetask__update_status(task_id, "Testing")
3. SlashCommand("/test {task_id}")  # ← ОБЯЗАТЕЛЬНО ВЫПОЛНИТЬ!
4. СРАЗУ после завершения тестов → перейти к Этапу 4
```

### Этап 4: TESTING → CODE REVIEW

**Триггер**: Тесты прошли успешно

**Действия (выполнять НЕМЕДЛЕННО):**
```
1. mcp__claudetask__append_stage_result(task_id, "Testing", "Tests passed")
2. mcp__claudetask__update_status(task_id, "Code Review")
3. SlashCommand("/PR {task_id}")  # ← ОБЯЗАТЕЛЬНО ВЫПОЛНИТЬ!
4. СРАЗУ после PR → перейти к Этапу 5
```

### Этап 5: CODE REVIEW → PR → DONE

**Триггер**: Код прошёл review (команда /PR завершилась успешно)

**Действия (выполнять НЕМЕДЛЕННО):**
```
1. mcp__claudetask__append_stage_result(task_id, "Code Review", "Review approved")
2. mcp__claudetask__update_status(task_id, "PR")
3. SlashCommand("/merge {task_id}")  # ← ОБЯЗАТЕЛЬНО ВЫПОЛНИТЬ СРАЗУ!
   # /merge автоматически:
   # - Мержит PR в main
   # - Удаляет worktree
   # - Останавливает сессию
   # - Обновляет статус на Done
4. Перейти к следующей задаче в очереди
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- НЕ говорить "awaiting user verification"
- НЕ ждать пользователя
- СРАЗУ выполнять /merge после успешного code review

---

## 🔄 ОБРАБОТКА ОШИБОК

### Тесты провалились:
```
1. mcp__claudetask__append_stage_result(task_id, "Testing", "Tests failed: {причина}")
2. mcp__claudetask__update_status(task_id, "In Progress")
3. SlashCommand("/start-develop")  # вернуться к разработке
```

### Code Review провален:
```
1. mcp__claudetask__append_stage_result(task_id, "Code Review", "Review failed: {причина}")
2. mcp__claudetask__update_status(task_id, "In Progress")
3. SlashCommand("/start-develop")  # вернуться к разработке
```

---

## ⚡ КЛЮЧЕВЫЕ КОМАНДЫ ДЛЯ AUTO MODE

| Переход | Команда | Когда выполнять |
|---------|---------|-----------------|
| Analysis → In Progress | `/start-develop` | СРАЗУ после analysis |
| In Progress → Testing | `/test {task_id}` | СРАЗУ после implementation |
| Testing → Code Review | `/PR {task_id}` | СРАЗУ после успешных тестов |
| Code Review → PR → Done | `/merge {task_id}` | СРАЗУ после /PR (auto-triggered) |

**⚠️ ВАЖНО:** В AUTO режиме `/merge` должен вызываться автоматически из `/PR` после успешного code review!

---

## 🚨 ЧЕГО НЕ ДЕЛАТЬ В AUTO MODE

❌ **НЕ ПИСАТЬ**: "Ready for testing. Waiting for your command."
❌ **НЕ ПИСАТЬ**: "Please run /test when ready."
❌ **НЕ СПРАШИВАТЬ**: "Should I proceed to the next stage?"
❌ **НЕ ЖДАТЬ**: Пользовательского ввода между этапами
❌ **НЕ ОСТАНАВЛИВАТЬСЯ**: После каждого статуса

---

## ✅ ЧТО ДЕЛАТЬ В AUTO MODE

✅ **СРАЗУ ВЫПОЛНЯТЬ** команды без вопросов
✅ **АВТОМАТИЧЕСКИ ПЕРЕХОДИТЬ** к следующему статусу
✅ **ЗАПУСКАТЬ** slash commands программно
✅ **ПРОДОЛЖАТЬ** до завершения задачи или ошибки
✅ **ИНФОРМИРОВАТЬ** пользователя о прогрессе (но не ждать ответа)

---

## 📊 ПРИМЕР ПОЛНОГО AUTO WORKFLOW

```
[Сессия началась]
→ Проверяю настройки... manual_mode = false
→ Получаю очередь задач...
→ Найдена задача #42 в статусе Backlog

[Этап 1: Analysis]
→ Обновляю статус на Analysis
→ Создаю worktree для task-42
→ Делегирую анализ business-analyst...
→ Делегирую архитектуру systems-analyst...
→ Analysis завершён

[Этап 2: Development]
→ Сохраняю stage result для Analysis
→ Обновляю статус на In Progress
→ Выполняю /start-develop
→ Разработка завершена

[Этап 3: Testing]
→ Сохраняю stage result для In Progress
→ Обновляю статус на Testing
→ Выполняю /test 42
→ Тесты прошли успешно

[Этап 4: Code Review]
→ Сохраняю stage result для Testing
→ Обновляю статус на Code Review
→ Выполняю /PR 42
→ PR создан, review пройден

[Этап 5: Done]
→ Сохраняю stage result для Code Review
→ Выполняю merge
→ Очищаю ресурсы
→ Обновляю статус на Done

[Следующая задача]
→ Проверяю очередь...
```

---

## 🔧 КАК ВЫПОЛНЯТЬ SLASH COMMANDS

Используй инструмент SlashCommand:

```python
# Правильно - выполняем команду
SlashCommand("/test 42")

# Правильно - выполняем PR
SlashCommand("/PR 42")

# НЕПРАВИЛЬНО - просто говорим о команде
"You can now run /test 42"  # ← НЕ ДЕЛАЙ ТАК!
```

---

## 🎯 КРИТЕРИИ УСПЕХА AUTO MODE

AUTO режим работает правильно когда:

1. ✅ Задачи автоматически проходят все статусы
2. ✅ Slash commands выполняются без участия пользователя
3. ✅ Stage results сохраняются для каждого перехода
4. ✅ Пользователь видит только прогресс, не запросы на действие
5. ✅ Ошибки обрабатываются автоматически (возврат к разработке)
6. ✅ После завершения задачи автоматически берётся следующая
