# 📦 Установочная система ClaudeTask Framework

## 🎯 Созданные файлы

### Основные скрипты установки:

1. **`install.sh`** - Главный установочный скрипт
   - Проверяет системные требования
   - Устанавливает все зависимости (backend, frontend, MCP)
   - Создает виртуальные окружения
   - Выполняет миграции базы данных
   - Генерирует конфигурационные файлы
   - Создает скрипты запуска

2. **`start.sh`** - Скрипт запуска (создается автоматически)
   - Запускает backend на порту 3333
   - Запускает MCP server на порту 8000
   - Запускает frontend на порту 3334
   - Логи сохраняются в `logs/`

3. **`stop.sh`** - Скрипт остановки (создается автоматически)
   - Останавливает все сервисы
   - Освобождает порты
   - Завершает все процессы

4. **`quick-install.sh`** - Быстрая установка с GitHub
   - Клонирует репозиторий
   - Запускает install.sh
   - Одна команда для полной установки

## 📚 Документация:

1. **`INSTALLATION.md`** - Краткая инструкция по установке
2. **`INSTALL_GUIDE.md`** - Подробное руководство по установке
3. **`QUICK_START.md`** - Руководство по быстрому старту
4. **`README_INSTALLER.md`** - Этот файл (описание установочной системы)

## 🚀 Использование

### Вариант 1: Локальная установка

Если репозиторий уже клонирован:

```bash
cd claude-code-feature-framework
chmod +x install.sh
./install.sh
```

### Вариант 2: Установка с GitHub

Клонировать и установить:

```bash
git clone https://github.com/YOUR_USERNAME/claude-code-feature-framework.git
cd claude-code-feature-framework
chmod +x install.sh && ./install.sh
```

### Вариант 3: Одна команда (будущая функция)

Когда репозиторий будет на GitHub:

```bash
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/claude-code-feature-framework/main/quick-install.sh | bash
```

## 🔧 Что делает install.sh

### Этап 1: Проверка требований
```bash
✓ Python 3.10+
✓ Node.js 18+
✓ npm 9+
✓ git
```

### Этап 2: Backend установка
```bash
1. Создает venv в claudetask/backend/venv
2. Устанавливает Python пакеты (FastAPI, SQLAlchemy, etc.)
3. Выполняет миграции базы данных
4. Создает .env файл
```

### Этап 3: MCP Server установка
```bash
1. Создает venv в claudetask/mcp_server/venv
2. Устанавливает MCP пакеты
3. Настраивает сервер
```

### Этап 4: Frontend установка
```bash
1. Устанавливает npm пакеты (React, Material-UI, etc.)
2. Создает .env файл
3. Подготавливает к запуску
```

### Этап 5: Создание скриптов
```bash
1. Создает start.sh - запуск всех сервисов
2. Создает stop.sh - остановка всех сервисов
3. Создает logs/ - директория для логов
```

## 📂 Структура после установки

```
claude-code-feature-framework/
├── install.sh              # ✓ Главный установщик
├── start.sh                # ✓ Создается автоматически
├── stop.sh                 # ✓ Создается автоматически
├── quick-install.sh        # ✓ Для GitHub установки
│
├── logs/                   # ✓ Создается автоматически
│   ├── backend.log
│   ├── frontend.log
│   └── mcp.log
│
├── claudetask/
│   ├── backend/
│   │   ├── venv/           # ✓ Создается автоматически
│   │   ├── data/           # ✓ Создается автоматически
│   │   │   └── claudetask.db
│   │   ├── .env            # ✓ Создается автоматически
│   │   └── requirements.txt
│   │
│   ├── frontend/
│   │   ├── node_modules/   # ✓ Создается автоматически
│   │   ├── .env            # ✓ Создается автоматически
│   │   └── package.json
│   │
│   └── mcp_server/
│       ├── venv/           # ✓ Создается автоматически
│       └── setup.py
│
└── docs/
    ├── INSTALLATION.md         # ✓ Краткая инструкция
    ├── INSTALL_GUIDE.md        # ✓ Подробный гайд
    ├── QUICK_START.md          # ✓ Быстрый старт
    └── README_INSTALLER.md     # ✓ Этот файл
```

## 🎨 Возможности установщика

### ✅ Что уже работает:

- Проверка системных требований
- Автоматическая установка всех зависимостей
- Создание виртуальных окружений Python
- Миграции базы данных
- Генерация конфигурационных файлов
- Создание скриптов запуска/остановки
- Логирование всех операций

### 🚀 Планируемые улучшения:

- Поддержка Docker установки
- Автоматическое обновление
- Выбор портов при установке
- Интерактивная конфигурация
- Проверка обновлений
- Бэкап/восстановление данных

## 🐛 Устранение неполадок

### Проблема: Порты заняты

```bash
./stop.sh  # Освободить порты
```

### Проблема: Ошибки Python пакетов

```bash
# Переустановить backend
cd claudetask/backend
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема: Ошибки npm

```bash
# Переустановить frontend
cd claudetask/frontend
rm -rf node_modules package-lock.json
npm install
```

### Проблема: База данных не создается

```bash
cd claudetask/backend
source venv/bin/activate
python migrations/migrate_add_custom_instructions.py
```

## 📊 Время установки

**Примерное время на разных системах:**

- **macOS (M1/M2)**: 2-3 минуты
- **macOS (Intel)**: 3-5 минут
- **Linux (Ubuntu)**: 3-5 минут
- **Windows (WSL2)**: 5-7 минут

*Зависит от скорости интернета и производительности системы*

## 🔐 Безопасность

Установщик:
- ✅ Не требует sudo/root
- ✅ Устанавливает только в текущую директорию
- ✅ Создает изолированные виртуальные окружения
- ✅ Не модифицирует системные пакеты
- ✅ Использует официальные пакеты (PyPI, npm)

## 📝 Логи установки

Все операции логируются:
- Backend установка: видна в терминале
- Frontend установка: видна в терминале
- MCP установка: видна в терминале
- Запуск сервисов: `logs/backend.log`, `logs/frontend.log`, `logs/mcp.log`

## 🤝 Вклад в развитие

Если вы хотите улучшить установщик:

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения в `install.sh`
4. Протестируйте на разных платформах
5. Создайте Pull Request

## 📞 Поддержка

При проблемах с установкой:

1. Проверьте системные требования
2. Посмотрите логи в `logs/`
3. Проверьте [INSTALL_GUIDE.md](INSTALL_GUIDE.md)
4. Создайте issue на GitHub

## 📄 Лицензия

MIT License - свободное использование и модификация

---

**Создано для упрощения установки ClaudeTask Framework** 🚀
